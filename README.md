## Grants Reporting App (v2)

A Next.js app that visualizes government grant data from a SQL Server database. The UI (Material UI + Recharts) consumes in-app API routes that run SQL queries and return aggregated data for charts and KPIs.
